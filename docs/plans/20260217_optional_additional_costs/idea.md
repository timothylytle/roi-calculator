# Optional Additional costs

When using the embed feature on both calculators(revenue and CX), I woudl like the Additional cost field to be optional, off by default,   when selected the field will not visible and the value will be zero